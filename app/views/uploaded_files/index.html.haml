- title = (current_user.admin? ? t('.admin_title') : t('.title') )

%header.entry-header
  = content_title(title, user: current_user)

.entry-content
  #uploaded_files_list
    = render partial: 'uploaded_files_list'

  %p.info= t('.cannot_edit_or_delete_reason')

  %hr
    .text-center
      = link_to t('uploaded_files.new.title'),
                new_user_uploaded_file_path,
                remote: true,
                class: 'btn btn-sm btn-light new-category-button'

%div.no-more-major-changes
  %p There are User Checklists based on this, so no more major changes are allowed.
  %p If you need to make changes, you can clone this one, which will set it to 'no longer in use', and then edit the new (cloned) one.
